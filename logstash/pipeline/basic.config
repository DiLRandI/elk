input {
  file {
    path => "/usr/share/logstash/pipeline/data/node-exporter-lite.log"
    codec => "json"
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "node-exporter-lite-%{+YYYY.MM.dd}"
  }
}